@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    Layout = "_Layout";
    ViewData["Title"] = "Home Page";

    //DateTime data = (DateTime)ViewData["Data"];
    var data = ViewBag.Data;
    var contact = new Contact { Email = "info@antoniopelleriti.it", Tel = "123.4567890" };

    var messaggio = TempData["Messaggio"];
    TempData["Messaggio"] = "un altro messaggio";
}

<div class="text-center">
    <h1 class="display-4">@ViewData["Messaggio"]</h1>
    <h2>oggi è giorno @data.ToShortDateString()</h2>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>

<partial name="_Contacts" model="@contact"></partial>

<hr />

@Html.Partial("_Contacts", contact)


<a asp-action="PassData" asp-route-msg="@ViewData["Messaggio"]">pass data</a>

<h2>TempData: @messaggio</h2>
